# Local game assets

This directory contains every image and audio file referenced through `/manus-storage/` by the latest build. It is included so the project runs in a standard local Vite server without the Manus storage proxy.

The local verification returned HTTP 200 for the coastal map, evacuation tunnel, and storm audio after `pnpm build` and `pnpm preview`.
