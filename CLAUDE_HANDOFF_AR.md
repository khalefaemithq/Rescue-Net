# تسليم مشروع «شبكة الإنقاذ» إلى كلود

## المهمة

أنت تتسلم نموذج لعبة متصفح 2D عربية RTL باسم **شبكة الإنقاذ**. المطلوب ليس إعادة كتابة اللعبة من الصفر، بل إصلاح النسخة الحالية وتحسينها حتى تفي بطلبات المالك أدناه، ثم إرجاع **ملف ZIP كامل للكود فقط** مع تقرير اختبار مختصر. اللعبة React 19 + Vite + TypeScript، والخريطة SVG خفيفة، وهدفها الهاتف والمتصفح أولاً.

> **قاعدة الصدق:** لا تقل إن الصوت أو التوقيت أو الرسم «يعمل» لمجرد وجود كود له. يجب تشغيل الجولة فعليًا، والتأكد من النتيجة المرئية، واختبار الصوت في متصفح يمكنه إخراج الصوت أو توضيح حد الاختبار بدقة.

## تشغيل المشروع

```bash
pnpm install
pnpm dev
pnpm test
pnpm check
pnpm test:interaction
pnpm test:performance
pnpm build
```

يعمل التطوير عادة على المنفذ 3000. المشروع لا يحتاج قاعدة بيانات لميكانيكا اللعبة الحالية. لا تضف ملفات صور أو صوت ضخمة إلى مجلدات العميل؛ إن أضفت أصولًا فعلية فاجعلها صغيرة، مرخصة أو مولّدة بوضوح، ودوّن المصدر والحجم.

## الملفات المهمة

| الملف | الغرض |
|---|---|
| `client/src/pages/Home.tsx` | واجهة React، خريطة SVG، سيارة الإسعاف، الأثر المرئي للعاصفة، بطاقة النتيجة، والتحكم اللمسي. |
| `client/src/game/engine.ts` | منطق الجولة: السكان، الخطوط، الرحلات، الجسر، الترقيات، العاصفة والفوز/الخسارة. |
| `client/src/game/audio.ts` | Web Audio للعواء والمحرك والمطر والريح والرعد؛ يحوي مراحل العاصفة وتوقيت الرعد والبرق. |
| `client/src/index.css` | كل التصميم والحركات وتجاوب الهاتف. |
| `client/src/game/engine.test.ts` | اختبارات Vitest لمنطق اللعبة وتوقيت مراحل العاصفة. |
| `scripts/qa-interaction.mjs` | اختبار Chromium لسحب الخطوط، السيارة، الطوارئ، الجسر، النتيجة، العاصفة والسيارة. |
| `scripts/qa-performance.mjs` | فحص جهوزية شاشة 375×812 وأعداد العقد والمركبات. |
| `ASSETS.md` | سجل الأصول المستخدمة وروابط التخزين. |
| `STRUCTURE.md` و`PLAN.md` و`MEMORY.md` | تاريخ البنية والقرارات وقيود الاختبار. |

## ما هو موجود ويعمل على مستوى الكود والاختبار

توجد خريطة عربية متجاوبة، رسم مسارات بالسحب من نفق الإخلاء، ثلاث وجهات، سيارات ذهابًا وإيابًا، ترقية السعة ومسار الطوارئ، توقف عند إنهاء الجميع، خسائر عند العاصفة، وترقية جسر عند انقطاع طريق الميناء. نجحت آخر دورة آلية في اختبارات المنطق والأنواع والتفاعل والأداء والبناء. قياس الأداء عند 375×812 كان DOMContentLoaded قريبًا من 151ms في بيئة الاختبار فقط.

يوجد في النسخة أيضًا تصميم إسعاف SVG أكثر احمرارًا بلا صليب، نظام Web Audio اصطناعي خفيف لعواء الإسعاف/المحرك/المطر/الريح/الرعد، وجدول عاصفة من ست مراحل كل 30 ثانية. يبدأ الرعد عند 18 ثانية ثم كل 24 ثانية؛ كل ثالث حدث مزدوج؛ والبرق يظهر بعد الرعد بـ3.5 ثوانٍ. هذا يطابق طلب المالك الأخير، لكنه **لم يُثبت بالاستماع اليدوي على هاتف فعلي**.

## التقييم الصريح: لماذا اعتبر المالك النتيجة فاشلة؟

المالك محق في أن معظم التحسينات السابقة كانت أقرب إلى تنفيذ تقني أولي منها إلى النتيجة المرئية/السمعية المقنعة التي طلبها. السيارة تغيرت بوضوح، بينما العاصفة والبرق والصوت لم تظهر للمالك بالشكل أو التوقيت المتوقعين. الفجوة ليست في غياب النية أو غياب بعض الكود؛ بل في ضعف التحقق النهائي على جهاز حقيقي، وفي الاعتماد على لقطات واختبارات Headless لا تسمع الصوت ولا تحكم على الإحساس البصري أثناء الحركة.

المشهد الحالي للعاصفة ما زال SVG بسيطًا: غيوم وسطور مطر وبرق قصير. لا يبدو كعاصفة طبيعية عالية الجودة كما يتوقع المالك. كذلك العواء مولَّد بـWeb Audio ولا يعد تسجيلًا واقعيًا فعليًا. لذلك **لا تسوّق الموجود كإنجاز نهائي**؛ اعتبره هيكلًا قابلًا للتحسين.

## طلبات المالك التي يجب إنجازها فعليًا

### 1. سيارة الإسعاف

يجب أن تبقى سيارة إسعاف SVG بلا أي صليب أو علامة طبية مركزية. اجعل الغالب هو الأحمر الواقعي مع شريط أبيض واضح، نوافذ وعجلات جيدة، وإضاءة أعلى المركبة. لا تجعل لها هالة سوداء أو `filter: drop-shadow` أو خلفية مدمجة. يجب أن تبقى كبيرة ومقروءة على الهاتف، وتتبع اتجاه الخط ذهابًا وإيابًا من دون انقلاب غير صحيح.

### 2. صوت الإسعاف

استبدل العواء الحالي بواحد مقنع وهادئ. يفضل استخدام ملف صوت قصير مرخّص/مولّد ومضغوط جدًا (مثل WAV/OGG صغير) أو تركيب Web Audio أعلى جودة إن كان الناتج مسموعًا فعلًا. يجب أن يبدأ فقط بعد أول لمسة من اللاعب بسبب سياسات المتصفح، ويتكرر بوتيرة مناسبة أثناء وجود سيارات متحركة، ويختفي عند الإيقاف أو كتم الصوت أو انتهاء الجولة. أضف اختبارًا برمجيًا لتأكيد مسار التفعيل، ثم اختبر السماع يدويًا على هاتف فعلي.

### 3. العاصفة والرعد والبرق

احذف مظهر «خطوط مطر خلفية ضعيفة» السابق تمامًا. اصنع عاصفة ساحلية مرئية: كتلة غيوم داكنة متدرجة من جهة البحر، طبقات ضباب/رذاذ، أمواج مضطربة، وبرق متشعب له توهج واقعي قصير. لا تستخدم حركة سريعة أو متكررة تزعج العين؛ الحركة يجب أن تكون نادرة ومقصودة.

بعد بدء المهمة، شغّل طبقة صوت مطر ورياح خفيفة باستمرار. كل 24 ثانية تقريبًا يحدث رعد، وأحيانًا رعدان متقاربان. بعد **3–4 ثوانٍ** من الرعد، يظهر برق واضح على الخريطة. يجب أن يزيد الصوت في مراحل حقيقية كل 30 ثانية، وأن يصل أقصاه في آخر 30 ثانية. استخدم `getStormStage` و`getThunderCue` أو أعد تصميمهما مع اختبارات تحمي السلوك المقصود.

### 4. الخسارة والفوز

احتفظ بإخلاء الفوز الحالي أو حسّنه. عند الخسارة، لا تكتفِ ببطاقة نصية: أضف حركة قصيرة وهادئة توضح أن العاصفة قطعت الطريق/ابتلعت من تبقى، مع عداد خسائر صحيح. يجب ألا تتعارض الحركة مع بطاقة النتيجة أو تختفي خلفها على الهاتف.

### 5. الجسر والترقية

تأكد من التالي في جولة حقيقية: عندما ينقطع جسر الميناء، تتوقف اللعبة بوضوح، ويتعطل خط الميناء فقط، وتظهر ترقية **«الجسر المؤقت»** كخيار إلزامي واحد، ثم تعيد الخط للعمل عند اختيارها. اشرح ذلك بعبارة قصيرة مفهومة للمستخدم. لا تترك هذه الآلية كحدث مبهم أو غير ظاهر.

## معايير قبول إلزامية

| المجال | شرط القبول |
|---|---|
| السيارة | لا صليب، لا هالة سوداء، اللون الأحمر ظاهر، واتجاه صحيح في الذهاب والعودة على أي خط. |
| العواء | يبدأ بعد ضغط «ابدأ المهمة»، يسمع على هاتف فعلي، لا يتداخل بعنف، ويتوقف عند الكتم/التوقف/النهاية. |
| العاصفة | لا يظهر رسم المطر الخلفي القديم؛ توجد عاصفة ساحلية مفهومة، ومطر/رياح مستمران، ومراحل تصاعد كل 30 ثانية. |
| الرعد والبرق | رعد دوري، حدث مزدوج أحيانًا، وبرق متشعب مرئي بعد 3–4 ثوانٍ من الرعد. |
| الجسر | انهيار ظاهر + ترقية إجبارية واحدة + طريق يعمل بعد الترقية. |
| الخسارة | أنيميشن قصير مرئي في بطاقة النتيجة، مع أعداد صحيحة. |
| الهاتف | اختبار عند 375×812 دون قص عناصر التحكم أو بطاقة النتيجة. |
| الجودة | شغّل `pnpm test && pnpm check && pnpm test:interaction && pnpm test:performance && pnpm build` بنجاح. |

## قواعد لا تُكسر

- الواجهة عربية RTL.
- لا تحذف رسم المسارات اللمسي أو منطق الرحلات الصحيحة والسعة وترقية الجسر.
- حافظ على SVG وWeb Audio خفيفين قدر الإمكان؛ لا تبنِ Babylon أو محركًا ثلاثي الأبعاد لهذا النموذج.
- لا تضف شهادات أو تقييمات أو بيانات لاعبين مزيفة.
- لا تقل «اختُبر الصوت» إلا إذا استمعت إليه على جهاز فعلي أو وصفت بوضوح أن الاختبار برمجي فقط.
- استخدم أصولًا مسموحًا بها ومضغوطة، ولا تضف `node_modules` أو `dist` إلى ملف التسليم.

## ما يجب إرجاعه

أعد ملف ZIP يحتوي المشروع بعد التعديل، دون `node_modules` أو `dist` أو `.git`. أضف ملفًا باسم `IMPLEMENTATION_REPORT_AR.md` يذكر: الملفات المعدلة، طريقة اختبار الصوت الفعلية، نتائج أوامر الاختبار، لقطة هاتف للعاصفة أثناء التشغيل ولقطة للخسارة، وأي عنصر بقي غير مكتمل. لا تنشر الموقع بنفسك؛ المالك سيعيد الحزمة إلى Manus للنشر.

## مراجع مرفقة في الحزمة

تتضمن الحزمة وثيقة التصميم العربية الأصلية، دراسة الأفكار، وسكرينشوت المالك `references/26221.jpg`. اعتبر الصورة الأخيرة دليلًا على المشكلة التي لاحظها المالك، لا نموذجًا نهائيًا يجب نسخه حرفيًا.
